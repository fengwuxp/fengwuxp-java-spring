server:
  port: 8090
  servlet:
    context-path: /api

spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/study_db?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&serverTimezone=UTC
#  datasource:
#    type: com.alibaba.druid.pool.DruidDataSource
#    username: chenz
#    password: $KmlL&ipwaq3
#    url: jdbc:mysql://39.99.243.172:3306/pile_db?useUnicode=true&characterEncoding=utf8&autoReconnect=true&&useSSL=false
  #    druid:
  #      #监控配置
  #      web-stat-filter:
  #        enabled: true
  #        url-pattern: "/*"
  #        exclusions: "*.js,*.gif,*.jpg,*.bmp,*.png,*.css,*.ico,/druid/*"
  #        # 配置DruidStatViewServlet
  #      stat-view-servlet:
  #        url-pattern: "/druid/*"
  #        # IP白名单(没有配置或者为空，则允许所有访问)
  #        allow: 127.0.0.1,192.168.163.1
  #        # IP黑名单 (存在共同时，deny优先于allow)
  #        deny: 192.168.1.73
  #        #  禁用HTML页面上的“Reset All”功能
  #        reset-enable: false
  #        # 登录名
  #        login-username: druid
  #        # 登录密码
  #        login-password: 123456

  jpa:
    scan-packages: com.wuxp,com.oaknt,com.oak
    generate-ddl: true
    show-sql: true
    format-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL57Dialect
      hibernate.id.new_generator_mappings: false
      hibernate.dialect.storage_engine: innodb

logging:
  level:
    org.springframework: debug
#    org.hibernate: debug

